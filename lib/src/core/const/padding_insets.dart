import 'package:flutter/rendering.dart';

class PaddingInsets {
  final kLowPadding = const EdgeInsets.all(5);
  final kMediumPadding = const EdgeInsets.all(10);
  final kHighPadding = const EdgeInsets.all(14);
}
